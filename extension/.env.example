# Base URL del backend Spring Boot
VITE_API_BASE_URL=http://localhost:8080

# API key compartida para el backend.
# NOTA: en una extensión distribuida esto NO es un secreto fuerte (el bundle es inspeccionable);
# la protección real del coste de la IA es el rate limiting del backend.
VITE_API_KEY=
